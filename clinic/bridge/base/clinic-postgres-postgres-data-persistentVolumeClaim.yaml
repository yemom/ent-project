#! clinic-postgres-postgres-data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: clinic-postgres-postgres-data
    namespace: clinic
    labels:
        com.docker.compose.service: clinic-postgres
        com.docker.compose.volume: postgres_data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
